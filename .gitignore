# Volcados temporales de la API de Meta (se regeneran corriendo los scripts de analisis/)
a.json
d.json
gasto.json
sep.json

# 🔴 DATOS DEL BOT — conversations.json y orders.json tienen TELEFONOS y DIRECCIONES
# de clientes reales. Este repo es publico: nunca se suben.
bot/data/
bot/.env
.env

# 🔴 DATOS PRIVADOS — exports del Meta Business Agent, chats reales de clientes.
# Nombres, telefonos y direcciones. Este repo es PUBLICO: nunca se suben.
# Solo pueden salir de aca resumenes agregados y anonimizados.
datos-privados/
*.zip

# Cache de Python
__pycache__/
*.pyc
